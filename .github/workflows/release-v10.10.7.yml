name: publish-v10.10.7

on:
  push:
    branches:
      - master
    paths:
      - .github/workflows/release-v10.10.7.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Publish v10.10.7 release
        uses: softprops/action-gh-release@v2
        with:
          tag